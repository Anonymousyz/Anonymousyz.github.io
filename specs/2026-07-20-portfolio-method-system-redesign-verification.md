# 个人主页方法体系重构验证

- 日期：2026-07-20
- 分支：`codex/portfolio-redesign-20260719`
- 本地地址：`http://127.0.0.1:4326/`
- GitHub 推送：未执行
- 发布：未执行

## 自动检查

- `npm.cmd test`：56/56 通过。
- `npm.cmd run build`：通过，生成 15 个静态页面。
- `npm.cmd run verify:build`：通过，15 routes / 15 HTML。
- `npm.cmd run verify:links`：通过，站内链接、锚点和批准外链均符合约束。
- `git diff --check 08f14edf...HEAD`：通过。
- 路径逃逸、坏锚点与未知外链均有失败夹具；检查过程不请求真实外网。

## 浏览器检查

在 Codex 应用内浏览器中检查首页、方法总页、七篇方法文章、项目页和公开作品页，共 11 个路由、33 个“路由 × 视口”组合。

| 视口 | 首页 | 方法总页 | 七个子页 | 项目与作品 | 横向溢出 | 控制台错误 |
|---|---|---|---|---|---:|---:|
| 1440×900 | 通过 | 通过 | 通过 | 通过 | 0 px | 0 |
| 768×1024 | 通过 | 通过 | 通过 | 通过 | 0 px | 0 |
| 390×844 | 通过 | 通过 | 通过 | 通过 | 0 px | 0 |

检查结论：

- 首页顺序为首屏、两个身份、持续学习、分析主线、应用主线、项目、公开作品。
- 桌面端两个身份等宽；850px 以下按长期主义者、终身学习者的顺序改为单栏。
- 中文引文是身份区主视觉，英文引文与出处保持较小字号和弱化颜色。
- 持续学习使用通栏；两条主线各三项，在桌面端等宽、移动端按确认顺序堆叠。
- 方法总页标题层级为 H1“方法”、隐藏 H2“方法索引”、H3 两条主线；文章页为单一 H1 与连续 H2。
- 手机端菜单从 44px 触发器底部展开，实测间距 4px；主要链接高度为 40–172px。
- 页面没有卡片浮起、阴影墙、横向滚动或过度动效；减少动态偏好会取消箭头位移。

最终修订后再次抽查首页、关于页和公开作品页：桌面与 390×844 均无横向溢出，控制台错误为 0；关于页 H1 已统一为“两条主线”，当前导航使用 `aria-current`；跳转正文入口和焦点目标存在；手机菜单触发器为 44×44px，展开间距为 4px；导航及 10px 元数据不再使用低对比旧青绿色。

## 键盘与焦点

- 用键盘事件逐项聚焦品牌、身份出处、持续学习、方法入口、项目入口和公开作品；六类元素均成为活动元素并显示 2px 焦点轮廓。
- 390px 下读取渲染后的焦点顺序，共 23 个可交互目标；顺序与页面内容一致，主要目标高度不低于 40px。
- 当前应用内浏览器的原始 Tab/Shift+Tab 指令没有推动 `activeElement`，因此未把“完整连续物理按键遍历”记为已完成；焦点样式、DOM 顺序、触控尺寸和代表性键盘聚焦已分别验证。

## 文案与边界

- 最终盲审的 5 项 Important 已全部关闭；复审结论为 Ready，无 Critical 或 Important。
- 方法文章中的“我”由 96 次降至 27 次，七篇不再共用统一六节结构。
- 七篇方法文章均连接至少一个具体项目和一项具名 GitHub 公开作品。
- 应用侧缺少公开产物支撑的动作已改为当前做法、检查项或条件表述，没有虚构项目证据。
- “专家委员会”只表示按不同项目角色列问题；需要专业结论时另请相应人员复核。
- 内部功能版本、正式 UAT、预生产与生产验收保持分开，未把受控 Demo 写成生产成果。
- `src`、`public`、`docs` 中 LinkedIn 扫描为 0。
- `个人 OS`、`能力矩阵`、`认知引擎`、`赋能`、`闭环`、`形成完整体系`、`证明能力`、`接受公开检查` 等禁用表述在公开源码和构建产物中为 0。

## 未包含

- 工业绿色微电网长案例扩写及项目附件补充。
- Git 历史改写。
- GitHub push。
- 网站发布。
